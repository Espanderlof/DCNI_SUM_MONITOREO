import { Routes } from '@angular/router';
import { PatientMonitorComponent } from './components/patient-monitor/patient-monitor.component';

export const routes: Routes = [
  { path: '', component: PatientMonitorComponent }
];